version: 1.0.{build}
configuration: Debug
platform: x64
build:
  project: УПКО.sln
  publish_nuget: true
  include_nuget_references: true
  verbosity: minimal
test:
  assemblies:
    only:
    - УПКО Инга/test.cpp
